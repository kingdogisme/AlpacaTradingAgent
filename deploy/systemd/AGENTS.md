# Systemd Boundary

This folder owns systemd unit files and operational service wiring.

## Rules

- Keep commands explicit and reviewable.
- Do not enable live broker execution by default.
- Document required environment variables outside unit files when possible.
