# Deploy Boundary

Deploy files own service definitions and runtime deployment glue.

## Rules

- Do not store secrets.
- Keep service commands aligned with CLI names.
- Execution services should default to safe paper-only behavior.
