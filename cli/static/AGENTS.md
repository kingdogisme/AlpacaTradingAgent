# CLI Static Assets Boundary

This folder stores static text assets used by CLI presentation.

## Rules

- Keep files small and presentation-only.
- Do not store runtime state, generated reports, or audit artifacts here.
- If command behavior depends on text content, add a CLI test.
