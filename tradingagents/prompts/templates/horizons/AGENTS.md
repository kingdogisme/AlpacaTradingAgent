# Horizon Prompt Templates Boundary

These templates define horizon-specific context and constraints.

## Rules

- Keep horizon definitions consistent with config: swing, position, trend.
- Research-only horizon constraints should not be treated as broker execution
  permission.
- Update tests when horizon language changes actionability behavior.
