# Graph Prompt Templates Boundary

These templates support legacy graph orchestration.

## Rules

- Keep compatibility with existing graph nodes.
- New V2 prompts should prefer layer-specific folders instead of expanding
  legacy graph prompt contracts.
