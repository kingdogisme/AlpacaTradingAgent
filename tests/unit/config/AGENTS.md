# Config Unit Tests Boundary

Tests here cover config defaults, environment overrides, and credential lookup
behavior.

## Rules

- Do not require real credentials.
- Keep secret redaction and env fallback behavior covered.
